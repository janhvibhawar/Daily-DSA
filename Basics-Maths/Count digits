#include<bits/stdc++.h>
using namespace std;

int countdigit(int n){
        int count=0;
        while(n!=0){
        int c=n%10;
        count++;
        n=n/10;
    }
    return count;
} 

int main(){
    int n;
    cin>>n;

    int result=countdigit(n);
    cout<<result;
}

    
